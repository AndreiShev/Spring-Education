server:
  port: 8080

spring:
  kafka:
    bootstrap-servers: localhost:9092

app:
  kafka:
    kafkaOrderTopic: "order-topic"
    kafkaOrderStatusTopic: "order-status-service"
    kafkaMessageGroupId: "kafka-message-group-id"
